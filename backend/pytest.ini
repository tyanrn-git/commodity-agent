[pytest]
testpaths = tests
pythonpath = .
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests that call live external APIs (excluded from default runs)
addopts = -m "not integration"
